install nest : npm i -g @nestjs/cli
new project : npx @nestjs/cli new <project-name>
cd <project-name>
start : npm run start
start and reload all the time : npm run start:dev

npm install firebase-admin @nestjs/config
npm install passport passport-jwt @nestjs/passport

CONFIGURATION firebase application
// Import the functions you need from the SDKs you need
import { initializeApp } from "firebase/app";
import { getAnalytics } from "firebase/analytics";
// TODO: Add SDKs for Firebase products that you want to use
// https://firebase.google.com/docs/web/setup#available-libraries

// Your web app's Firebase configuration
// For Firebase JS SDK v7.20.0 and later, measurementId is optional
const firebaseConfig = {
  apiKey: "AIzaSyDCpAUllWaCdiueLkxEezP5e7vFipr9HZU",
  authDomain: "wbcp-project1.firebaseapp.com",
  projectId: "wbcp-project1",
  storageBucket: "wbcp-project1.appspot.com",
  messagingSenderId: "1000629534336",
  appId: "1:1000629534336:web:1b2d89cacf9f8e6531b065",
  measurementId: "G-W27WMNZJKQ"
};

// Initialize Firebase
const app = initializeApp(firebaseConfig);
const analytics = getAnalytics(app);